<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="customer">
	<sql id="search">
		<where>
			<if test="search != null and keyword !='' and keyword != null">
				${search} LIKE CONCAT(CONCAT('%',#{keyword}),'%')
			</if>
		</where>	
	</sql>
	<sql id="all">
		<where>
			<if test="keyword !='' and keyword !=null">
				custid LIKE CONCAT(CONCAT('%',#{keyword}),'%') OR name LIKE CONCAT(CONCAT('%',#{keyword}),'%') 
				OR address LIKE CONCAT(CONCAT('%',#{keyword}),'%') OR phone LIKE CONCAT(CONCAT('%',#{keyword}),'%') 
			</if>
		</where>
	</sql>
	<select id="total" resultType="Integer">
		SELECT COUNT(*) total FROM customer 
		<include refid="all"></include>
		
	</select>
	<select id="list" resultType="Customer">
		SELECT * 
		FROM(SELECT ROWNUM row_num, sub.* 
		FROM(SELECT * from customer
		<include refid="all"></include>
		order by custid) sub)
		
		<![CDATA[
		WHERE row_num > ((#{page} - 1) * #{perPage}) AND row_num <= (#{page} * #{perPage})
		]]>
	</select>
	
	<select id="item" resultType="Customer">
		SELECT * FROM customer
		WHERE custid=#{custid}
	</select>
	
	<insert id="add">
		INSERT INTO customer (custid, name, address, phone)
		VALUES (#{custid}, #{name}, #{address}, #{phone})
	</insert>
	
	<update id="update">
		UPDATE customer
		SET name=#{name}, address=#{address}, phone=#{phone}
		WHERE custid=#{custid}
	</update>
	
	<delete id="delete">
		DELETE FROM customer
		WHERE custid=#{custid}
	</delete>
</mapper>	